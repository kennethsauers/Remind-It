
/**
 * Class that will hold the User data once the user is logged in.
 * This information will be held throughout the instance of the
 * application and stored in the AuthenticationService
 */
export class User {
  // Essentials for authentication
  id: String;
  username: String;

  // anything beyond this might need an elvis operator
}
